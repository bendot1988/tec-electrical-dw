<section class="renewable-section" aria-labelledby="renewable-heading">
	<div class="renewable-grid">
		<div class="image-collage" data-reveal="fade-left" data-reveal-stagger="120">
			<figure class="image-card tall" data-reveal="zoom">
				<img
					src="/renewable-battery.png"
					alt="Sigenergy home battery storage system installed by Tech Electrical"
					loading="lazy"
				/>
			</figure>

			<div class="image-stack">
				<figure class="image-card" data-reveal="zoom">
					<img
						src="/renewable-inverter.png"
						alt="Growatt inverter and battery equipment installed in a home"
						loading="lazy"
					/>
				</figure>
				<div class="experience-card" data-reveal="blur">
					<strong>15+ yrs</strong>
					<span>Solar &amp; battery installs</span>
				</div>
			</div>
		</div>

		<div class="copy-column" data-reveal="fade-right" data-reveal-stagger="100">
			<p class="eyebrow" data-reveal="blur">Renewable Energy</p>
			<h2 id="renewable-heading" data-reveal>Solar, Battery &amp; EV -- designed and installed by experts</h2>
			<p class="intro" data-reveal>
				We handle every step in-house: consultation, system design, installation and ongoing optimisation.
				Approved installers using premium kit from Sigenergy, Growatt and more.
			</p>

			<ul class="feature-list" aria-label="Renewable services" data-reveal>
				<li>
					<span class="icon" aria-hidden="true">☀</span>
					<span>
						<strong>In-roof &amp; On-roof Solar</strong>
						<small>Sleek integrated panels designed for new builds and retrofits.</small>
					</span>
				</li>
				<li>
					<span class="icon" aria-hidden="true">▢</span>
					<span>
						<strong>Battery Storage</strong>
						<small>Maximise self-consumption with optional whole-house backup.</small>
					</span>
				</li>
				<li>
					<span class="icon" aria-hidden="true">⚡</span>
					<span>
						<strong>EV Charging</strong>
						<small>Smart chargers integrated with your solar for free miles.</small>
					</span>
				</li>
			</ul>

			<div class="cta-row" data-reveal="zoom">
				<a class="primary" href="#quote">Get Your Solar Quote</a>
				<a class="secondary" href="/services">Speak to a Specialist</a>
			</div>
		</div>
	</div>
</section>

<style>
	.renewable-section {
		background: #fff;
		padding: 5.5rem var(--site-content-padding);
	}

	.renewable-grid {
		max-width: 1200px;
		margin: 0 auto;
		display: grid;
		grid-template-columns: 1fr 1.1fr;
		gap: 2.2rem;
		align-items: center;
	}

	.image-collage {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 0.75rem;
	}

	.image-card {
		margin: 0;
		overflow: hidden;
		border-radius: 0.9rem;
		box-shadow: 0 14px 35px -24px rgba(15, 23, 42, 0.5);
	}

	.image-card img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		display: block;
	}

	.image-card.tall {
		min-height: 410px;
	}

	.image-stack {
		display: grid;
		gap: 0.75rem;
	}

	.image-stack .image-card {
		min-height: 320px;
	}

	.experience-card {
		background: linear-gradient(135deg, #114bc2, #0e3a98);
		color: #fff;
		padding: 1rem 1rem 1.1rem;
		border-radius: 0.7rem;
	}

	.experience-card strong {
		display: block;
		font-size: 2rem;
		line-height: 1;
	}

	.experience-card span {
		display: block;
		margin-top: 0.4rem;
		font-size: 0.95rem;
		color: rgba(248, 250, 252, 0.92);
	}

	.eyebrow {
		margin: 0;
		text-transform: uppercase;
		letter-spacing: 0.08em;
		font-size: 0.78rem;
		font-weight: 700;
		color: #3567ab;
	}

	h2 {
		margin: 0.6rem 0 0.9rem;
		font-size: clamp(2rem, 3vw, 3rem);
		line-height: 1.12;
		color: #0f172a;
	}

	.intro {
		margin: 0;
		font-size: 1.06rem;
		line-height: 1.6;
		color: #475569;
	}

	.feature-list {
		margin: 1.6rem 0 1.7rem;
		padding: 0;
		list-style: none;
		display: grid;
		gap: 0.9rem;
	}

	.feature-list li {
		display: grid;
		grid-template-columns: 2.2rem 1fr;
		gap: 0.7rem;
		align-items: start;
	}

	.icon {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: 2rem;
		height: 2rem;
		border-radius: 0.55rem;
		background: #fff6e5;
		color: #e58c00;
		font-size: 1rem;
		font-weight: 700;
	}

	.feature-list strong {
		display: block;
		font-size: 1rem;
		color: #0f172a;
	}

	.feature-list small {
		display: block;
		margin-top: 0.15rem;
		font-size: 0.94rem;
		color: #64748b;
	}

	.cta-row {
		display: flex;
		flex-wrap: wrap;
		gap: 0.75rem;
	}

	.cta-row a {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		border-radius: 0.6rem;
		padding: 0.82rem 1.25rem;
		font-weight: 700;
		font-size: 0.92rem;
	}

	.primary {
		background: linear-gradient(120deg, #f59e0b, #f97316);
		color: #fff;
	}

	.secondary {
		border: 1px solid #dbe1eb;
		background: #fff;
		color: #1f2937;
	}

	@media (max-width: 980px) {
		.renewable-grid {
			grid-template-columns: 1fr;
		}
	}

	@media (max-width: 680px) {
		.renewable-section {
			padding: 4.2rem var(--site-content-padding);
		}

		.image-collage {
			grid-template-columns: 1fr;
		}

		.image-card.tall,
		.image-stack .image-card {
			min-height: 290px;
		}
	}
</style>
