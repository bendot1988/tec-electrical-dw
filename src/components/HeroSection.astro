<section class="hero" id="quote">
	<div class="hero-inner">
		<div class="hero-copy" data-reveal="left" data-reveal-stagger="110">
			<p class="eyebrow" data-reveal="blur">MCS Certified Solar Installers</p>
			<h1 data-reveal>
				Solar, Battery & EV
				<span>Powering Leicestershire homes</span>
			</h1>
			<p class="subtitle" data-reveal>
				Cut your energy bills with premium in-roof solar, whole-home battery backup and smart EV charging.
				Designed and installed by a 15-year electrical team you can trust.
			</p>

			<ul class="benefits" aria-label="Benefits" data-reveal>
				<li>In-roof and on-roof PV</li>
				<li>Battery storage + backup</li>
				<li>EV charging integration</li>
				<li>Slash energy bills</li>
			</ul>

			<div class="cta-group" data-reveal="zoom">
				<a class="primary" href="#quote-form">Get My Free Solar Quote</a>
				<a class="secondary" href="tel:+441160000000">Call Us Now</a>
				<a class="secondary" href="https://wa.me/447000000000">WhatsApp</a>
			</div>
		</div>

		<aside class="quote-card" id="quote-form" aria-label="Quick quote form" data-reveal="right">
			<h2>Get Your Quick Quote</h2>
			<p>Fill out the form and we&apos;ll call you within 1 hour.</p>
			<form class="quote-form">
				<div class="field-grid">
					<label>
						<span>Name *</span>
						<input type="text" name="name" placeholder="John Smith" required />
					</label>
					<label>
						<span>Phone *</span>
						<input type="tel" name="phone" placeholder="07..." required />
					</label>
				</div>
				<label>
					<span>Postcode *</span>
					<input type="text" name="postcode" placeholder="LE11 2XX" required />
				</label>
				<label>
					<span>Brief Job Description *</span>
					<textarea name="job" rows="4" placeholder="Please describe your electrical requirements..." required />
				</label>
				<button type="submit">Get My Free Quote</button>
				<small>We&apos;ll call you within 1 hour</small>
			</form>
		</aside>
	</div>
</section>

<style>
	.hero {
		position: relative;
		overflow: hidden;
		padding: 4.2rem var(--site-content-padding) 5rem;
		background:
			linear-gradient(102deg, rgba(8, 28, 88, 0.9) 0%, rgba(10, 41, 110, 0.82) 45%, rgba(16, 66, 156, 0.72) 100%),
			url('/hero-solar-roof.png');
		background-size: cover;
		background-position: center;
	}

	.hero::before {
		content: '';
		position: absolute;
		inset: 0;
		background: linear-gradient(180deg, rgba(9, 26, 80, 0.08), rgba(9, 26, 80, 0.35));
	}

	.hero-inner {
		position: relative;
		z-index: 1;
		max-width: 1200px;
		margin: 0 auto;
		display: grid;
		grid-template-columns: 1.15fr 0.95fr;
		gap: 2rem;
		align-items: start;
	}

	.hero-copy {
		color: #fff;
		max-width: 640px;
	}

	.eyebrow {
		display: inline-block;
		padding: 0.42rem 0.9rem;
		border-radius: 999px;
		font-size: 0.74rem;
		font-weight: 700;
		letter-spacing: 0.06em;
		text-transform: uppercase;
		background: rgba(148, 163, 184, 0.22);
		border: 1px solid rgba(255, 255, 255, 0.22);
	}

	h1 {
		margin: 1.1rem 0 1rem;
		font-size: clamp(2.1rem, 4.8vw, 4rem);
		line-height: 1.05;
		font-weight: 800;
		letter-spacing: -0.03em;
	}

	h1 span {
		display: block;
		margin-top: 0.3rem;
		color: #f59e0b;
		font-size: clamp(1.6rem, 3.2vw, 2.9rem);
	}

	.subtitle {
		margin: 0;
		font-size: 1.1rem;
		line-height: 1.6;
		color: rgba(241, 245, 249, 0.95);
		max-width: 580px;
	}

	.benefits {
		margin: 1.4rem 0 1.8rem;
		padding: 0;
		list-style: none;
		display: grid;
		grid-template-columns: repeat(2, minmax(170px, 1fr));
		gap: 0.7rem 1rem;
	}

	.benefits li {
		position: relative;
		padding-left: 1rem;
		font-size: 0.97rem;
		color: #f8fafc;
	}

	.benefits li::before {
		content: '•';
		position: absolute;
		left: 0;
		color: #f59e0b;
		font-size: 1.2rem;
		line-height: 1;
	}

	.cta-group {
		display: flex;
		flex-wrap: wrap;
		gap: 0.7rem;
	}

	.cta-group a {
		border-radius: 0.6rem;
		padding: 0.84rem 1.2rem;
		font-weight: 700;
		font-size: 0.92rem;
	}

	.primary {
		background: linear-gradient(120deg, #f59e0b, #f97316);
		color: #fff;
	}

	.secondary {
		background: rgba(15, 23, 42, 0.32);
		color: #fff;
		border: 1px solid rgba(255, 255, 255, 0.28);
	}

	.quote-card {
		padding: 1.4rem;
		background: #fff;
		border-radius: 1rem;
		box-shadow: 0 25px 50px -26px rgba(15, 23, 42, 0.65);
	}

	.quote-card h2 {
		margin: 0;
		font-size: 2rem;
		color: #0f172a;
	}

	.quote-card p {
		margin: 0.35rem 0 1rem;
		color: #475569;
		font-size: 0.95rem;
	}

	.quote-form {
		display: grid;
		gap: 0.78rem;
	}

	.field-grid {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 0.7rem;
	}

	label {
		display: grid;
		gap: 0.35rem;
		font-size: 0.86rem;
		font-weight: 600;
		color: #334155;
	}

	input,
	textarea {
		width: 100%;
		padding: 0.65rem 0.72rem;
		border: 1px solid #d6deea;
		border-radius: 0.48rem;
		font: inherit;
	}

	button {
		border: 0;
		border-radius: 0.55rem;
		padding: 0.78rem 1rem;
		background: linear-gradient(120deg, #f59e0b, #f97316);
		color: #fff;
		font-size: 0.95rem;
		font-weight: 700;
		cursor: pointer;
	}

	.quote-form small {
		text-align: center;
		color: #64748b;
	}

	@media (max-width: 980px) {
		.hero-inner {
			grid-template-columns: 1fr;
		}

		.hero {
			padding-top: 3rem;
		}
	}

	@media (max-width: 640px) {
		h1 {
			font-size: clamp(2rem, 8vw, 3rem);
		}

		h1 span {
			font-size: clamp(1.4rem, 6vw, 2.1rem);
		}

		.subtitle {
			font-size: 1rem;
		}

		.benefits {
			grid-template-columns: 1fr;
		}

		.field-grid {
			grid-template-columns: 1fr;
		}
	}
</style>
